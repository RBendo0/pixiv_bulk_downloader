Campione ID:
	N_MAX = 39724
	ID_MIN = 118099
	ID_MAX = 145654652
	ID_25% = 124983990
	ID_SPLIT = 125000000

Sistema Bucket Hype:
	DENSITY_HYPE_A = 0.16223786
	DENSITY_HYPE_B = 1.09912234
	DENSITY_HYPE_SCALE = 3.0

	Condizione:
		ID < ID_SPLIT 

	Parametri: 
		_A = DENSITY_HYPE_A * ID_MAX
		_B = DENSITY_HYPE_B * ID_MAX
		DENSITY_HYPE(ID) = DENSITY_HYPE_SCALE * _A / (N_MAX * EXP((ID - _B) / _A))		

	Formula: ID_GROUP_HYPE(ID) = ID / (GROUP_SIZE * DENSITY_HYPE(ID))

	Identificatore FOLDER_NAME = "H_" + "ID_GROUP_HYPE(ID)"

Sistema Bucket Stable:
	GROUP_SIZE = 500
	DENSITY_STABLE = 550
	BUCKET_GUARD_BAND = 1000	

	Condizione:
		ID >= ID_SPLIT 

	Parametri: 
		HYPE_MAX_BUCKET = MAX(ID_GROUP_HYPE(0), ID_GROUP_HYPE(ID_PLIT)) = 1563047
		BUCKET_STABLE_OFFSET = HYPE_MAX_BUCKET + BUCKET_GUARD_BAND

	Formula: ID_GROUP_STABLE(ID) = BUCKET_STABLE_OFFSET + ID / (GROUP_SIZE * DENSITY_STABLE)

	Identificatore FOLDER_NAME = "S_" + "ID_GROUP_STABLE(ID)"




